// concat :: [[a]] -> [a]
function concat(a) {
  return a.reduce(reduceConcat, []);
}

function reduceConcat(acc, item) {
  return Array.isArray(item) ? acc.concat(item) : acc;
}

module.exports = concat;
